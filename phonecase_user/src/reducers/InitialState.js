import React from "react";

export const config = config => ({
        activeWindow:0,
        page: 0,
        error:false,
        errorMessage:undefined,
        username:undefined,
        password:undefined
    }

);